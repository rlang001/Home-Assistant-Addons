# Automatically generated do not edit

# Static hosts
# openobserve logs


{{ range .hosts }}
log {
    source(s_net_udp);
    source(s_net_tcp);
    filter({{ .host }});
    destination({{ .host }});
    flags(flow-control);
};
{{ end }}
