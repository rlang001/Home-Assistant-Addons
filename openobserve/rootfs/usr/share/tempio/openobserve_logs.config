# Automatically generated do not edit

# Static hosts
# openobserve logs


{{ range .hosts }}
log {
    source(s_net_udp);
    source(s_net_tcp);
    filter(f_{{ .host }});
    destination(d_{{ .host }});
    flags(flow-control);
};
{{ end }}
